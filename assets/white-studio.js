(()=>{'use strict';const reduced=matchMedia('(prefers-reduced-motion: reduce)');let paused=false,frame=0;const sections=[...document.querySelectorAll('main>section')];const items=[];sections.forEach(section=>{section.querySelectorAll('h1,h2,.section-heading>p,.opening-content>p,.closing-content>p,.solution-card,.achievement-card,.process-grid article,.portrait,.case-carousel,.ref-structure figure,.ref-ai img,.case-detail-visual,.case-detail-text article,.feature-case,.range-card,.range-grid article,.calc-layout,.output-card,.service-blueprint,.service-index,.service-grid article,.media-grid,.faqs,.output-grid' ).forEach((el,i)=>{if(el.parentElement.closest('[data-parallax]'))return;el.dataset.parallax='';items.push({el,section,depth:[.022,-.018,.03][i%3]});});});const button=document.createElement('button');button.className='motion-toggle';button.type='button';button.setAttribute('aria-pressed','false');button.textContent='Отключить движение';const host=document.querySelector('footer .wrap')||document.querySelector('main');host?.append(button);function render(){frame=0;const off=paused||reduced.matches;const mobile=innerWidth<700;sections.forEach(s=>{const r=s.getBoundingClientRect();s.style.setProperty('--draft-shift',off?'0px':Math.max(-35,Math.min(35,(innerHeight/2-r.top-r.height/2)*.05))+'px');});items.forEach(({el,section,depth})=>{const r=section.getBoundingClientRect();const y=off?0:Math.max(-16,Math.min(16,(innerHeight/2-r.top-r.height/2)*depth))*(mobile?.4:1);el.style.setProperty('--parallax-y',y+'px');});button.hidden=reduced.matches;button.textContent=paused?'Включить движение':'Отключить движение';button.setAttribute('aria-pressed',String(paused));}function schedule(){if(!frame)frame=requestAnimationFrame(render);}button.addEventListener('click',()=>{paused=!paused;document.body.classList.toggle('motion-paused',paused);schedule();});addEventListener('scroll',schedule,{passive:true});addEventListener('resize',schedule);reduced.addEventListener('change',schedule);render();})();